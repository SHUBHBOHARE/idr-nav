version: '3.8'

services:
  idr-nav-backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
      - "3000:3000"
    environment:
      - DATABASE_URL=sqlite:///./idr_nav.db
    volumes:
      - ./data:/app/data
    restart: always
